spring:
  application:
    name: item-service # 服务名称
  profiles:
    active: local
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.174.128:8848 # Nacos 服务注册地址
      config:
        server-addr: 192.168.174.128:8848 # Nacos 配置中心地址
        file-extension: yaml # 配置文件后缀
        shared-configs:
          - dataId: shared-jdbc.yaml
          - dataId: shared-log.yaml
          - dataId: shared-swagger.yaml
          - dataId: shared-seata.yaml